import {
    $sessionsCount,
    $totalTime,
    $averageSession,
    $workBreakRatio,
} from "./dom-manager.js";

let statsToSave = {
    workSessions: 0,
    totalWorkTime: 0,
    averageSession: 0,
    workBreakRation: 0,
}

/* function saveStatistics() {
    if ()
    workSessions++;
} */